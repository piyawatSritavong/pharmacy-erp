**Business Flow — ข้อเสนอเพิ่มเติมจากการวิเคราะห์ระบบ Ocha เดิม**
---
> เอกสารนี้เป็น **ข้อเสนอ** เท่านั้น ยังไม่ได้แก้ `business-flow.md`
> ที่มา: ไฟล์ `.mhtml` 35 หน้าในโฟลเดอร์ `Ocha/` (ระบบ Ocha POS เดิมของร้าน)
> วิธีอ่าน: แต่ละหัวข้อระบุ **ทำอะไร / อยู่เมนูไหน / ต้องพึ่งอะไร / ข้อควรระวัง**
> รายการที่ Ocha มีแต่เรามีอยู่แล้ว ถูกตัดออกหมด — เหลือเฉพาะที่ใหม่จริง

---

## สรุปผู้บริหาร

วิเคราะห์ 35 หน้าจาก Ocha พบฟีเจอร์ที่ **ใหม่จริง** 12 รายการ, ที่ **ควรข้าม** 11 รายการ

**อัปเดต 2026-08-08 — ผลการตัดสินใจ 3 เรื่อง:**
- ❌ `ราคาหลายระดับต่อสินค้า` — **ปฏิเสธ** (ราคาขายเฉพาะสาขาตอบโจทย์แล้ว) ปิดเรื่อง
- ❌ `ช่องทางชำระเงินแบบบัตร` — **ปฏิเสธ** (ธุรกิจนี้รับเงินสด/โอน/ผสม เท่านั้น) ตัดออกจากเอกสารแล้ว
- ⚠️ `พักบิล` — **ตรวจแล้วไม่ซ้ำทั้งหมด** ใบเสนอราคาแทนได้เฉพาะฝ่ายสำนักงาน
  แต่ `branch_pos` ถูก 403 เข้าใบเสนอราคาไม่ได้เลย → **ยังรอการตัดสินใจ** (ดูรายละเอียดด้านล่าง)

จึงเหลือฟีเจอร์ที่ยังเปิดให้พิจารณา **11 รายการ**

ฟีเจอร์ใหม่ที่ประเมินว่า**คุ้มค่าที่สุด 3 อันดับแรก**:
1. `รอบลิ้นชักเงินสด` — ปิดช่องโหว่การควบคุมเงินสดหน้าร้าน (ตอนนี้เราไม่มีเลย)
2. `ส่วนลด` (catalog ส่วนลด) — ตอนนี้เรามีแค่เพดานส่วนลดต่อสินค้า ไม่มีส่วนลดที่ตั้งไว้ใช้ซ้ำ
3. `รายงานสินค้าคงคลังต้นงวด-ปลายงวด` — ต่อยอดจาก movement ที่มีอยู่แล้ว ต้นทุนต่ำ

---

# ข้อเสนอเพิ่มเติม

## เมนู: รายงาน (ขยายของเดิม)

`รอบลิ้นชักเงินสด`
- ควบคุมเงินสดในลิ้นชักต่อรอบการขายของแต่ละสาขา — เปิดรอบด้วยเงินทอนตั้งต้น ปิดรอบด้วยการนับเงินจริง
- บันทึก: เงินตั้งต้น, ยอดขายเงินสด, เงินคืนลูกค้า, รายการเงินเข้า, รายการเงินออก, จำนวนเงินที่ควรมี, จำนวนเงินที่นับได้จริง, **ส่วนต่าง**
- มีประวัติรอบย้อนหลัง และประวัติเงินเข้า/เงินออกแยกต่างหาก
- ตั้งเวลาปิดรอบอัตโนมัติประจำวันได้ (Ocha ตั้งไว้ 21:00)
- **อยู่เมนู**: `รายงาน` (รายงาน) + ปุ่มเปิด/ปิดรอบอยู่ฝั่ง POS
- **ต้องพึ่ง**: ตาราง `invoice_payments` ที่มีอยู่แล้ว (แยกเงินสด/เงินโอนได้แล้ว), ต้องเพิ่มตารางรอบและรายการเงินเข้า-ออก, ต้องเพิ่ม permission ใหม่ (เช่น `cash_drawer.manage`)
- **ข้อควรระวัง**: ต้องผูกรอบกับ "สาขา" ไม่ใช่ "เครื่อง" เพราะโครงสร้างเราเป็น multi-branch ไม่ได้ลงทะเบียนอุปกรณ์รายเครื่องแบบ Ocha; ถ้าสาขาหนึ่งมีหลายเครื่องขายพร้อมกัน ต้องตัดสินใจว่าใช้รอบเดียวร่วมกันหรือแยกตามผู้ใช้

`รายงานสินค้าคงคลัง (ต้นงวด-ปลายงวด)`
- สรุปการเคลื่อนไหวสต๊อกต่อช่วงเวลา: จำนวนต้นงวด → ขายออก → เพิ่ม → ลด → จำนวนปลายงวด พร้อมเกณฑ์แจ้งเตือนและธงสินค้าใกล้หมด
- ต่างจาก `สต๊อกจริง/สต๊อกผี` ที่แสดงยอด ณ ปัจจุบัน — อันนี้คือการกระทบยอดตามงวด
- **อยู่เมนู**: `รายงาน` หรือทำเป็น dataset ใหม่ใน `Generate Report`
- **ต้องพึ่ง**: `inventory_movements` ที่มีครบอยู่แล้ว — เป็นการ query ใหม่ ไม่ต้องเก็บข้อมูลเพิ่ม
- **ข้อควรระวัง**: ต่ำมาก น่าจะทำเป็น dataset ใน Generate Report ได้เลย ไม่ต้องสร้างหน้าใหม่

`เปรียบเทียบช่วงเวลา`
- ทุกรายงานของ Ocha เทียบกับ: ช่วงก่อนหน้า / สัปดาห์ที่แล้ว / เดือนที่แล้ว / ปีที่แล้ว ได้ในคลิกเดียว
- **อยู่เมนู**: เป็นความสามารถของ `Generate Report` ไม่ใช่เมนูใหม่
- **ต้องพึ่ง**: report-builder ที่มีอยู่ (มี time_field/time_range แล้ว) — เพิ่มการ query ซ้ำอีกช่วงแล้วแสดงส่วนต่าง %
- **ข้อควรระวัง**: ต้องระวังเรื่อง performance เพราะกลายเป็น query 2 เท่า; และ `สรุปสิ้นเดือน` ห้ามแตะตามกฎที่ล็อกไว้

`รายงานการคืนเงิน / รายการที่ถูกยกเลิก / บิลที่ถูกลบ`
- ติดตามบิลที่ถูกยกเลิก คืนเงิน หรือลบ พร้อมผู้ทำรายการและเวลา — ใช้ตรวจสอบการทุจริตหน้าร้าน
- **อยู่เมนู**: `รายงาน`
- **ต้องพึ่ง**: ฐานข้อมูลมี `invoice_status IN ('issued','cancelled')` อยู่แล้วแต่ยังไม่เคยถูกใช้ (0 รายการ) และยังไม่มี flow ยกเลิกบิลใน UI
- **ข้อควรระวัง**: **ต้องนิยามให้ชัดว่า "ยกเลิกบิล" ต่างจาก "ลบ" อย่างไร** เพราะ Global Rules เขียนว่ามีแต่ CRUD ไม่มีการเก็บเข้าคลัง — การยกเลิกบิลขายไม่ใช่การ archive แต่เป็นการกลับรายการทางบัญชี (ต้องคืนสต๊อกด้วย) ควรระบุเป็นกฎแยก

---

## เมนู: คลังสินค้า (ขยายของเดิม)

`ส่วนลด`
- catalog ส่วนลดที่ตั้งไว้ล่วงหน้าและเรียกใช้ซ้ำได้ตอนขาย (Ocha มี: ส่วนลด 1/5/10/50/100/500 บาท)
- 2 ชนิด: **ส่วนลดปกติ** (ใช้ได้ทุกสินค้า) และ **ส่วนลดตามประเภทรายการ** (ผูกกับหมวดสินค้าที่เลือก)
- กำหนดเป็นเปอร์เซ็นต์หรือจำนวนเงิน; เว้นว่างได้เพื่อให้กรอกตอนขาย
- Ocha ระบุชัดว่า **ส่วนลดถูกคิดก่อนคำนวณภาษี**
- **อยู่เมนู**: `คลังสินค้า` (คู่กับ `หมวดสินค้า`) หรือ `ตั้งค่า`
- **ต้องพึ่ง**: `หมวดสินค้า` ที่มีอยู่ (สำหรับส่วนลดตามประเภท), และ POS ต้องเลือกส่วนลดตอนขายได้
- **ข้อควรระวัง**: ตอนนี้เรามีแค่ `max_discount_amount` = **เพดาน**ส่วนลดต่อสินค้า ซึ่งเป็นคนละเรื่อง — ต้องตัดสินใจว่าส่วนลดจาก catalog ยังต้องเคารพเพดานเดิมหรือไม่ (แนะนำ: ต้องเคารพ ไม่งั้นเพดานไร้ความหมาย)

`จัดลำดับหมวดสินค้า`
- กำหนดลำดับการแสดงผลของหมวดสินค้า (Ocha ใช้เลข 1-200 น้อยไปมาก ค่าเริ่มต้น 200 เลขซ้ำเรียงตามเวลาสร้าง)
- **อยู่เมนู**: `คลังสินค้า` → `หมวดสินค้า` (เพิ่มฟิลด์ ไม่ใช่เมนูใหม่)
- **ต้องพึ่ง**: ตาราง `product_categories` — ปัจจุบัน**ไม่มีคอลัมน์ลำดับ** ต้องเพิ่ม migration 1 คอลัมน์
- **ข้อควรระวัง**: ต่ำมาก เป็นการเพิ่มฟิลด์ธรรมดา

---

## เมนู: ตั้งค่า (ขยายของเดิม)

`นโยบายการปัดเศษและค่าบริการ`
- **การปัดเศษ**: เลือกโหมด (ปัดขึ้น / ปัดลง / ปัดใกล้สุด / ไม่ปัด) และหน่วยการปัด (฿0.25 / ฿0.50 / ฿1) — Ocha ปัดตอนชำระเงินสด
- **ค่าบริการ**: เปิด/ปิดการเก็บค่าบริการ ซึ่งคิด**ก่อน**ภาษี
- **อยู่เมนู**: `ตั้งค่า` (แท็บใหม่ หรือรวมกับที่มีอยู่)
- **ต้องพึ่ง**: `app_settings` มี `vat_rate`, `company_name`, `company_tax_id`, `company_address` อยู่แล้ว — เพิ่ม key ใหม่ได้เลย; แต่ต้องแก้ logic คำนวณเงินใน `priceLines`/`checkout`
- **ข้อควรระวัง**: **กระทบการคำนวณเงินโดยตรง** ซึ่งเชื่อมกับ `สรุปสิ้นเดือน` ที่ห้ามแตะ — ถ้าเปิดฟีเจอร์นี้ต้องตรวจว่า month-end ยังกระทบยอดตรงหรือไม่ ควรทำเป็นงานแยกที่มีการทดสอบเฉพาะ

`รูปแบบใบเสร็จและเครื่องพิมพ์`
- ตั้งค่ารูปแบบใบเสร็จ, จำนวนสำเนาที่พิมพ์ให้พนักงาน, และตัวเลือกแสดงปุ่ม "ออกบิล" บนหน้าขาย
- **อยู่เมนู**: `ตั้งค่า`
- **ต้องพึ่ง**: ระบบพิมพ์ปัจจุบันเป็น `window.print()` ของเบราว์เซอร์ (ไม่มี driver เครื่องพิมพ์)
- **ข้อควรระวัง**: ส่วนที่ทำได้จริงคือ **รูปแบบ/จำนวนสำเนา** เท่านั้น; การสั่งเครื่องพิมพ์ความร้อนโดยตรงต้องมี native app/driver ซึ่งเกินสถาปัตยกรรมเว็บปัจจุบัน — ควรจำกัดขอบเขตให้ชัด

---

## เมนู: ระบบ (ขยายของเดิม)

`อุปกรณ์/เครื่องขาย`
- ทะเบียนเครื่องที่ใช้ขาย (Ocha มี SUNMI_T2, iPad, iPhone ฯลฯ) และใช้เป็น**ตัวกรองในทุกรายงาน**
- ใช้ตอบคำถาม "เครื่องไหนขายได้เท่าไร" และผูกกับรอบลิ้นชักเงินสด
- **อยู่เมนู**: `ระบบ` (คู่กับ `ตั้งค่า`)
- **ต้องพึ่ง**: ต้องเพิ่มตารางอุปกรณ์ + เก็บ device id ตอน checkout
- **ข้อควรระวัง**: มีประโยชน์เฉพาะเมื่อสาขาหนึ่งมีหลายเครื่องขายพร้อมกัน — ถ้าแต่ละสาขามีเครื่องเดียว ข้อมูลนี้ซ้ำกับ "สาขา" ที่มีอยู่แล้ว **ควรยืนยันกับหน้างานก่อนว่าจำเป็นไหม**

---

## เมนูใหม่ (ต้องตัดสินใจว่าเอาหรือไม่)

`สมาชิก` (ระบบสมาชิก/CRM)
- ทะเบียนลูกค้าประจำ + รายงาน `ยอดขายตามสมาชิก`
- **อยู่เมนู**: เมนูใหม่ระดับบนสุด หรืออยู่ใต้ `ใบเอกสาร`
- **ต้องพึ่ง**: ต้องมีตารางลูกค้า, ผูกกับใบขาย, และ POS ต้องเลือกสมาชิกตอนขาย — ปัจจุบันเราเก็บแค่ `customer_name` เป็น text อิสระ
- **ข้อควรระวัง**: เป็นโมดูลใหม่ทั้งก้อน (ข้อมูล + UI + POS flow + รายงาน) **ใหญ่ที่สุดในรายการนี้**; ถ้าร้านไม่ได้ทำโปรโมชันสมาชิกอยู่แล้ว แนะนำให้เลื่อนไปทีหลัง

`พักบิล / เปิดบิลค้างไว้` — ⚠️ **ตรวจสอบแล้ว: ซ้ำบางส่วน ยังต้องตัดสินใจ**
- สร้างและบันทึกรายการขายไว้ก่อน ค่อยชำระเงินทีหลัง (ลูกค้ายังเลือกของอยู่ / รอญาติมาจ่าย)
- **ผลการตรวจสอบว่า `ใบเสนอราคา` ครอบคลุมหรือไม่:**
  - ✅ **กลไกตรงกัน** — `CreateQuotation` ไม่แตะสต๊อกเลย (ไม่มีการเรียก FEFO/lockAndApplyStock)
    มีสถานะ `draft → converted → cancelled` และแปลงเป็นใบขายพร้อมตัดสต๊อกทีหลังได้
    ดังนั้นสำหรับ **ฝ่ายสำนักงาน/แอดมิน** ใบเสนอราคาแทนการพักบิลได้จริง
  - ❌ **แต่พนักงานขายหน้าร้านใช้ไม่ได้** — บทบาท `branch_pos` ไม่มีสิทธิ์ `quotation.manage`
    (ทดสอบจริง: `GET /quotations` และ `POST /quotations` ตอบ **HTTP 403**)
    และเมนูฝั่ง POS ก็ไม่มีรายการใบเสนอราคา
- **ช่องว่างที่เหลือ**: เคสที่ Ocha ออกแบบ `เปิดบิล` มาแก้ คือ**แคชเชียร์ที่เคาน์เตอร์**
  พักบิลลูกค้าที่ยังเลือกของอยู่ — ซึ่งเป็นบทบาทเดียวที่เข้าใบเสนอราคาไม่ได้เลย
- **ทางเลือกในการตัดสินใจ** (ยังไม่ได้ทำอะไร รอสั่ง):
  1. ให้สิทธิ์ `quotation.manage` (หรือสิทธิ์ใหม่ที่แคบกว่า) กับ `branch_pos` + เพิ่มเมนูฝั่ง POS
     → ต้นทุนต่ำสุด ใช้ของที่มีอยู่ แต่กระทบระบบสิทธิ์ที่ระบุว่า "ยืนยันแล้วว่าถูกต้อง"
  2. ทำ `พักบิล` เป็นฟีเจอร์แยกเฉพาะฝั่ง POS
  3. ไม่ทำ — ถ้าหน้าร้านไม่เคยต้องพักบิลจริง

---

# ❌ ปฏิเสธแล้ว (ปิดเรื่อง ไม่ต้องทำ)

`ราคาหลายระดับต่อสินค้า` — ❌ **ปฏิเสธแล้ว (ตัดสินใจ 2026-08-08)**
- Ocha รองรับหลายราคาต่อสินค้า 1 ตัว (หน้ารายการสินค้าแสดง "2 ราคา", "3 ราคา", "4 ราคา")
  และในระบบสต๊อกแต่ละราคาถูกนับเป็นรายการสต๊อกแยกกัน เช่น `... (ราคาปกติ)` กับ `... (ราคา 2)`
- **สถานะ: ไม่รับเข้าระบบ** — เพราะ `ราคาขายเฉพาะสาขา` ที่มีอยู่แล้วตอบโจทย์นี้ครบ
  แต่ละสาขาตั้งราคาของตัวเองทับราคาตั้งต้นได้อยู่แล้ว จึงไม่ต้องมีหลายราคาต่อสินค้า
- **ไม่ต้องทำอะไรต่อ**: ไม่รื้อฟื้น `retail_price` และไม่เพิ่มระบบราคาหลายระดับ
  กฎ "ราคาขายเดียว + override ระดับสาขา" ใน `business-flow.md` ยังคงเดิม

---

# ไม่ชัดเจน / น่าจะข้าม

รายการต่อไปนี้เจอใน Ocha แต่ประเมินว่าเป็นบริบทร้านอาหาร, เป็นเรื่องของผู้ขายซอฟต์แวร์,
หรือไม่มีความหมายกับร้านอุปกรณ์การแพทย์ — **ไม่ได้เดากฎธุรกิจให้**

| ฟีเจอร์ Ocha | เหตุผลที่ข้าม |
|---|---|
| `ส่วนประกอบ` (BOM/สูตรอาหาร) | แนวคิดวัตถุดิบ-สูตรของร้านอาหาร; ใน Ocha เองก็ดูเหมือนสะท้อนสินค้า 1:1 ไม่ได้ใช้จริง |
| `เมนูย่อย` (modifiers) | ตัวเลือกเสริมของเมนูอาหาร (หวานน้อย/เพิ่มชีส) ไม่ตรงกับอุปกรณ์การแพทย์ |
| `จำนวนโต๊ะ` / `การจัดการโต๊ะ` | ร้านอาหารล้วน ๆ |
| `เครื่องพิมพ์ครัว` / `ส่งออเดอร์เข้าครัว` | ร้านอาหารล้วน ๆ |
| `ประเภทบิล` (ทานที่ร้าน/สั่งกลับบ้าน) | ร้านอาหารล้วน ๆ |
| `QR สแกนสั่งอาหาร` | ร้านอาหาร + เป็นฟีเจอร์ขายเสริมของ Ocha |
| `ประเภทร้านและสินค้า` (ร้านหรู/จานด่วน/บุฟเฟ่ต์) | การจัดหมวดหมู่ร้านของ Ocha ไม่มีผลกับเรา |
| `สกุลเงิน` (หลายสกุล) | ร้านขายในไทยสกุลเดียว |
| `บิลของฉัน` / `ค่าบริการซอฟต์แวร์` | การเก็บค่าบริการรายปีของ Ocha เอง |
| `สั่งซื้อกระดาษความร้อน` / `คู่มือเครื่องพิมพ์` | ร้านค้าและเอกสารของผู้ขาย |
| `กฎหมาย` / `ส่งคำขอลบบัญชี` | นโยบายบัญชี SaaS ของ Ocha |
| `รายการละเอียดอ่อน` | หน้าจอมีแค่ เวลา/ผู้ใช้งาน/จำนวน/หมายเลขอ้างอิง — **ไม่พอจะสรุปได้ว่านิยาม "ละเอียดอ่อน" คืออะไร** ต้องถามหน้างาน |
| `ทิป` | Ocha มีช่องทิปในทุกรายงาน แต่ร้านอุปกรณ์การแพทย์ปกติไม่รับทิป — ถ้าไม่ใช้ ไม่ต้องทำ |

---

# ที่ Ocha มี และเรามีอยู่แล้ว (ไม่ต้องทำอะไร)

`สรุปยอดขาย`, `วิธีการชำระเงิน`, `ยอดขายตามผู้ใช้งาน`, `ยอดขายตามประเภท`, `รายการขายดี`,
`รายงานรายการขาย`, `ประวัติการขาย`, `ใบกำกับภาษีฉบับเต็ม` → ครอบคลุมด้วย `Generate Report`
+ `ประวัติการขาย` + dashboard ที่ทำไปแล้ว

`รายการสินค้าทั้งหมด` → `รายการสินค้า` | `ประเภทรายการ` → `หมวดสินค้า`
`รายการสินค้าคงคลัง` / `สต็อคเหลือน้อย` → `สต๊อกจริง`/`สต๊อกผี` + จุดแจ้งเตือนบนหน้า POS
`ประวัติ` (สต๊อก) → `inventory_movements` + `ประวัติระบบ`
`พนักงาน` / `ตั้งค่าสิทธิ์พนักงาน` → `ตั้งค่า` → `ผู้ใช้` + `บทบาทและสิทธิ์` (D11)
`ภาษี` (ข้อมูลผู้ประกอบการ) → `app_settings` มี `company_name` / `company_tax_id` / `company_address` / `vat_rate` แล้ว

---

# ข้อสังเกตเชิงสถาปัตยกรรม

1. **Ocha ไม่มีแนวคิด "โกดังกลาง"** — ทุกสาขาเป็นร้านเท่ากันหมด ระบบเรามี `WH` เป็นคลังหลัก
   ที่ขายไม่ได้ ฟีเจอร์ที่ยกมาต้องรองรับความต่างนี้ (เช่น รอบลิ้นชักเงินสดไม่ควรมีที่โกดัง)
2. **Ocha ไม่มีแนวคิด "สต๊อกผี"** — เป็นของเราเอง ฟีเจอร์ที่ยกมาต้องระบุว่าใช้กับ bucket ไหน
3. **Ocha ไม่มีแนวคิดขายออนไลน์แยกสาขา** — กฎ `online_sales_enabled` เฉพาะคณาเภสัชเป็นของเรา
   ไม่มีอะไรใน Ocha ที่ขัดกับกฎนี้
4. **Ocha ผูกทุกอย่างกับ "เครื่อง" (device)** ส่วนเราผูกกับ "สาขา + ผู้ใช้"
   ทุกฟีเจอร์ที่ยกมาต้องแปลงแกนจาก device → branch/user
